@model PackManagementSystem.Entities.Park
@{
    ViewData["Title"] = "Delete-Park";
}
<div>
    <h4 style="alignment: center; color: maroon; padding-bottom: 20px; padding-left: 20px"> Delete the selected Park? </h4>
    <div>
        <table class="table">
            <tbody>
            <tr>
                <th>
                    
                    <form asp-action="DeletePark">
                        <input type="hidden" asp-for="Id"/>
                        <input type="submit" value="Confirm" class="btn btn-danger">
                    </form>
                </th>
                <th><a asp-action="AllParks" class="btn badge-success">Decline</a></th>
            </tr>
            </tbody>
        </table>
    </div>
</div>
